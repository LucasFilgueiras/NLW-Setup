import { Text } from "react-native"

export const HabitsEmpty = () => {
    return (
        <Text className="text-zinc-400 text-base">
            Ainda não foi criando nenhum hábito neste dia
        </Text>
    )
}